package com.example.onlinecoursebackend.service;

import com.example.onlinecoursebackend.db.entity.course.Course;
import com.example.onlinecoursebackend.db.entity.user.User;
import com.example.onlinecoursebackend.dto.ApiResponse;

import java.util.UUID;

public interface CourseService {
// ApiResponse<Void> addCourse(AddCourseResponseDto req, User user);

// ApiResponse<Void> updateCourse(UUID courseId, EditCourseRequestDto req, UUID userId);

// ApiResponse<Void> deleteCourse(UUID courseId, UUID userId);

// ApiResponse<List<CourseResponseDto>> list(UUID userId);

// ApiResponse<CourseResponseDto> getById(UUID courseId, UUID userId);

}
